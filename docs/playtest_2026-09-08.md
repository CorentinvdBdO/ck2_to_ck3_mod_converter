# Playtest notes — 2026-09-08 (user, first In Game build 82ec406)

Verbatim findings, with the lane that owns each fix.

| # | finding | owner / fix |
|---|---|---|
| 1 | Launcher: "the path in this mod's descriptor file is invalid"; direct launch works | claudespace `push_mod.sh` (lane launcher-path): the launcher (Windows app under Proton) cannot read an absolute Linux `path=`; register as `path="mod/<name>"` with a symlink inside the Proton user dir `mod/` folder |
| 2 | Bookmark characters all stuck top-left, not clickable | `bookmarks` step (lane playtest-fixes): `position = { 0 0 }` for every character; vanilla spreads them over the bookmark canvas |
| 3 | Bookmark screen has no paper map (expected) | assets; later |
| 4 | Bookmarks: one group "Faerun" + dates, vanilla groups by date then region | not needed unless broken |
| 5 | Zoomed-out flat map shows the real-world paper map | lane map-ui: neutral flatmap texture |
| 6 | Map too high: exits the "tabletop", 3D assets collide with the table; Faerûn is top-heavy | lane map-ui: table model / camera / defines for non-2:1 maps (EK2 8256×5504 does it) |
| 7 | Title icons (CoA/names) displaced far to the SW of their land | lane map-ui: vanilla `gfx/map/map_object_data` locators (per vanilla province id) inherited; generate ours from barony centroids |
| 8 | Characters selectable; traits are a mix of vanilla and converted (expected) | — |
| 9 | Vanilla CK2 traits with no CK3 equivalent (e.g. `envious`) are missing: "vanilla removed content should be replaced, not dropped" | `traits` step (lane playtest-fixes): `none` and `approx` rows are ported as new traits |
| 10 | Zoom in/out lag where 3D assets collide with the map | same as 6 |
| 11 | Northern quarter of the map (table end) unreachable | camera bounds; lane map-ui |
| 12 | Crash when selecting the Silk Road situation | lane tc-template: shadow/neutralise vanilla situations, struggles, legends, POIs, flavorization, template lists |
| 13 | Ugly: missing assets, single race look (expected); ugly map | assets lane later; lane map-fidelity (research) |
| 14 | Cultures are empty: no traditions (user expected placeholder traditions) | `cultures` step (lane playtest-fixes): placeholder traditions from a table keyed by CK2 flags/heritage |
| 15 | Vanilla events/situations can crash on missing titles (expected) | lane tc-template |
| 16 | Basically playable; Voronoi baronies good enough most of the time | — |

Research questions raised (lane `map-fidelity`): how different are CK2 and CK3 map data (elevation, painted
terrain, assets and placement); can barony barycentres come from placed assets (wonder, city, port); can CK3
assets be placed from CK2's; can height/terrain be painted in vanilla style from CK2 data + vanilla statistics +
procedural generation.

Direction: before events, reach a **blank TC state** (no vanilla content naming vanilla map objects), as the
existing TC templates do.
